# config.yaml — Hugo configuration
# Le Togo en famille

baseURL: "https://toogoodtogo.blog/"
languageCode: "fr"
title: "Le Togo en famille"
theme: "hugo-paper"

# Taxonomies (FR-3)
taxonomies:
  author: authors
  tag: tags

# Menus
menu:
  main:
    - name: "Auteurs"
      url: "/authors/"
      weight: 10
    - name: "École"
      url: "/tags/ecole/"
      weight: 15
    - name: "Archives"
      url: "/archives/"
      weight: 20
    - name: "À propos"
      url: "/a-propos/"
      weight: 30

# Paramètres du site
params:
  subtitle: ""
  # color removed — handled by CSS tokens (--color-bg in tokens.css)
  disableHLJS: true

# Permaliens
permalinks:
  posts: "/:year/:month/:slug/"

# RSS natif (FR-10) — aucune config supplémentaire requise
# Hugo génère automatiquement /index.xml

# Markup
markup:
  goldmark:
    renderer:
      unsafe: false
    extensions:
      typographer:
        disable: false
        leftDoubleQuote: "«&nbsp;"
        rightDoubleQuote: "&nbsp;»"
        leftSingleQuote: "‹&nbsp;"
        rightSingleQuote: "&nbsp;›"

# Build
buildDrafts: false
buildFuture: false
buildExpired: false

# Minification
minify:
  disableXML: true
  minifyOutput: false

# Pagination
pagination:
  pagerSize: 20

# Résumés
summaryLength: 70

# Copyright
copyright: "Famille — Sokodé, Togo"
